package com.opensource.docgrid.domain.collection.enums;

/**
 * 컬렉션(문서 그룹) 상태.
 * ACTIVE: 사용 중, ARCHIVED: 보관됨, DELETED: 삭제됨.
 */
public enum CollectionStatus {
    ACTIVE,
    ARCHIVED,
    DELETED
}
