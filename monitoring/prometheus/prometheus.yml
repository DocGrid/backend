global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: embedding-provider
    metrics_path: /metrics
    static_configs:
      - targets:
          - embedding-server:8000
